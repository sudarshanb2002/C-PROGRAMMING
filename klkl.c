// Online C compiler to run C program online
#include <stdio.h>
#include<string.h>
int main() {
   char a[10],b[10],c[10];
   int count=0;
   scanf("%s",a);
   scanf("%s",b);
   puts(a);puts(b);
    for(int i=0;a[i]!='\0'||b[i]!=0;i++){
        if(a[i]!=b[i]){
          for(int j=0;j<strlen(a);j++){

          }
        }
    }
    printf("%d",count);
    puts(c);

    return 0;
}
